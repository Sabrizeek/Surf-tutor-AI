@echo off
REM Start the Node.js backend server

echo Starting Backend Server...
echo Make sure MongoDB is running (if using MongoDB)
echo Make sure the Model Server is running on port 8000

npm start

